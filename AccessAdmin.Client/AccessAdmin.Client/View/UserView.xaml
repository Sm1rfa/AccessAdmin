<UserControl x:Class="AccessAdmin.Client.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccessAdmin.Client.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label Content="Choose system:" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <ComboBox ItemsSource="{Binding SystemsList}"
                  DisplayMemberPath="SystemName"
                  SelectedItem="{Binding SelectedSystem}"
            HorizontalAlignment="Left" Margin="10,41,0,0" 
            VerticalAlignment="Top" Width="272"/>
        <Label Content="Choose role:" HorizontalAlignment="Left" Margin="10,72,0,0" VerticalAlignment="Top"/>
        <ComboBox ItemsSource="{Binding RolesList}"
                  DisplayMemberPath="RoleName"
                  SelectedItem="{Binding SelectedRole}"
            HorizontalAlignment="Left" Margin="10,103,0,0" 
            VerticalAlignment="Top" Width="272"/>
        <Button Content="Request" Background="LimeGreen" Foreground="White" HorizontalAlignment="Left" Margin="207,142,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</UserControl>
